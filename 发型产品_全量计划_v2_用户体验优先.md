# “咋剪发”全量产品计划 V2｜用户真实体验优先版

> 文档版本：V2.0
> 更新日期：2026-08-10
> 文档性质：当前产品范围、长期方向与分阶段实施基线
> 原始灵感档案：`发型产品_全量Idea方案设想.md`（保留，不覆盖）

---

# 0. 本版重构结论

“咋剪发”不做社区，不做应用内发帖，不做评论、点赞、关注、私信、公开用户主页，也不做任何依赖陌生人互动的内容产品。

产品重新收敛为：

> **一套私人的理发决策、沟通、记录与长期记忆工具。**

用户在应用内完成：

```text
认识自己的头发
→ 找到值得尝试的方向
→ 形成 2～4 个私人候选
→ 判断现实可剪性
→ 生成理发师沟通卡
→ 记录真实理发结果
→ 沉淀复刻与避雷经验
→ 生成本地分享文件
→ 下一次不再从零开始
```

用户若想去小红书、微信或其他外部平台征求意见、发帖或分享，由应用在本地生成文件，用户自行保存和发布。应用不托管帖子，不接收外部评论，不建立公开内容库。

本版同时明确：

- 近期账号只做邮箱登录；微信、手机号登录冻结到正式商业化准备阶段。
- 应用内不做本应用自身的支付、订单、会员、积分、优惠券或购买消费记录。
- 外部工具可以负责购买和发码；应用只处理“兑换后拥有什么功能权益”。
- 不做兑换码后台、客服台、经营数据后台或内容审核后台。
- 理发师端和商家端不实现，只保留路线说明或静态占位。
- 现阶段 Vercel 只作为测试链接载体；任何正式商业使用必须切换到符合实际用途的托管方案。
- 不采用“任意兑换码经 AI 转成同一个固定密码”作为次数权益方案。共享密码只能用于封闭测试，无法可靠实现每位用户 N 次机会。
- 本计划统一将“N 次应用体验机会”定义为 **N 次成功的真实 AI 生成批次**，不是 N 次打开网页、登录或浏览本地功能。
- 一次生成批次固定为“同一张输入照片 + 同一个发型方向 + 最多 3 个可比较候选”；用户主动换方向或要求重新出图的微调属于新的生成批次。
- 私人理发档案中的理发价格、办卡和推销记录仍可作为可选本地字段；它们不属于本应用的支付、订单或消费系统，也不会上传到外部发码平台。

---

# 1. 一句话定义

> **剪前看看，剪时说清，剪后记住。下一次，不再从头开始。**

“咋剪发”帮助用户降低真实理发翻车概率，而不是只生成一张好看的发型图。

产品的核心价值由四部分组成：

1. **私人决策**：把参考图、过去成功记录和 AI 方案变成可比较的候选。
2. **现实沟通**：把“我想要这种感觉”翻译成理发师能快速理解的要求。
3. **真实复盘**：记录刚剪完、洗后和第 7 天的真实状态。
4. **长期记忆**：让成功经验可复刻，让失败经验不会再次发生。

AI 是增强这条闭环的能力，不是产品价值的唯一来源。

---

# 2. 永久边界与当前非目标

## 2.1 永久不做的站内社交

产品永久不提供：

- 用户发帖；
- 评论和回复；
- 点赞、关注、粉丝；
- 私信、群聊；
- 用户公开主页；
- 好友投票页、匿名票数或站内短评；
- 陌生人案例投稿；
- 公共理发师榜单；
- 公共店铺点评；
- 站内达人、热榜、话题和挑战；
- 任何需要内容审核、举报或社区治理的 UGC 功能。

现有好友投票功能与这一边界冲突，应从新产品结构中退出。替代方案是生成本地 A/B/C/D 对比图和可复制提问文案，由用户自行发到外部平台。

## 2.2 当前不做的商业功能

应用内不提供：

- 支付；
- 订单；
- 会员；
- 积分；
- 优惠券；
- 商品页；
- 购物车；
- 发票；
- 外部订单同步；
- 退款处理页面；
- 经营看板。

外部平台如何完成购买、发码、退款和订单管理，不属于本应用的产品功能。

## 2.3 当前不做的运营系统

- 不做独立兑换码管理后台；
- 不做客服工单系统；
- 不做用户行为增长看板；
- 不做营销自动化；
- 不做内容审核后台；
- 不做商家或理发师管理后台。

可以保留最低限度的技术日志，用于避免 AI 次数误扣、定位接口失败和执行数据删除。日志不得包含照片、明文兑换码、完整邮箱或用户输入的敏感文字。这属于可靠性基础设施，不是经营分析。

## 2.4 理发师端和商家端

当前不实现任何理发师或商家功能，包括：

- 理发师注册；
- 店铺入驻；
- 作品上传；
- 在线预约；
- 到店确认；
- 与用户聊天；
- 商家认领；
- 公开能力画像；
- B 端收费。

产品可以在“关于—产品路线”中显示一个静态说明：

> 理发师协作能力处于长期规划阶段，当前没有开放入口。

它不进入底部导航，不设置误导性的“立即入驻”按钮，也不创建空功能页面。

---

# 3. 目标用户与真实问题

## 3.1 核心用户

- 18 岁以上；
- 不限性别；
- 当前优先服务短发及中短发用户；
- 容易在刘海、两侧、顶部、鬓角或后脑沟通上翻车；
- 保存过很多参考图，但无法判断哪张适合现实中的自己；
- 曾经剪得满意，却无法准确复刻；
- 不愿意公开完整正脸，但愿意在外部平台征求意见；
- 希望减少每次理发重新解释的负担。

## 3.2 用户真正要完成的任务

用户不是为了“玩一张 AI 图”而来，而是希望完成以下任务：

1. 我下一次到底应该往哪个方向剪？
2. 这个方案在我的长度、发量和打理习惯下是否现实？
3. 理发师怎样才能在十秒内看懂我的重点？
4. 这次真实结果值不值得复刻？
5. 下次怎样避免同样的错误？
6. 我想去外部问朋友，但怎样降低直接露脸压力？

---

# 4. 产品完整私人闭环

```text
首次进入
→ 了解本地保存和照片边界
→ 游客建立最小头发档案
→ 选择本次目标方向
→ 添加 2～4 个候选
→ 查看现实可剪性与风险
→ 确定主方案和备选方案
→ 自动生成并编辑沟通卡
→ 可选生成本地外部分享图
→ 理发现场打开沟通卡
→ 剪后 60 秒完成最小记录
→ 洗后 / 第 7 天补充真实状态
→ 标记复刻或沉淀避雷规则
→ 下一次直接继承成功经验
```

核心数据关系必须始终连贯：

```text
个人档案
→ 理发计划
→ 候选方案
→ 最终目标
→ 沟通卡
→ 理发记录
→ 阶段照片
→ 标准发型 / 避雷规则
→ 下一次计划
```

应用的每个主页面都应回答一个问题：

> **用户现在最应该做的下一步是什么？**

---

# 5. 产品体验原则

## 5.1 先给价值，再要求登录

- 游客可以使用完整的本地建档、候选、沟通卡、记录、遮罩和导出功能。
- 首次进入不强制注册，也不展示长问卷。
- 只有在用户需要邮箱恢复、跨设备数据或真实 AI 次数时，才引导登录。

## 5.2 先问用户知道的，再逐渐补充

- 专业发质字段都允许选择“不知道”。
- 首次建档只收集决策必需信息。
- 照片、染烫史和更细字段在真正需要时补充。

## 5.3 现实判断优先于漂亮效果

任何 AI 结果都要同时说明：

- 当前长度够不够；
- 是否需要留长；
- 是否需要烫、染或吹风；
- 不打理时可能是什么效果；
- 最容易翻车的部位；
- 哪些只是造型和摄影效果。

## 5.4 失败不消耗用户信任

- AI 技术失败不扣体验次数；
- 图片不合格要给出明确修正方法；
- 弱网时保留用户已经填写的内容；
- 导出失败必须提供重试和替代保存路径；
- 不把系统错误写成用户操作错误。

## 5.5 所有分享先生成文件

- 应用不创建公开帖子或公开用户页面；
- 分享前必须让用户看到最终扁平化预览；
- 用户决定是否保存、是否发送、发送到哪里；
- 外部反馈不会被自动抓取回应用。

---

# 6. 信息架构与导航

底部导航保持四项：

## 6.1 首页

首页不是功能目录，而是根据用户状态展示唯一主操作。

状态示例：

- 未建档：`建立我的头发档案`；
- 已建档、无计划：`准备下次理发`；
- 有计划、候选不足：`添加候选`；
- 有 2～4 个候选：`确定主方案`；
- 已确定方案：`打开沟通卡`；
- 理发日期已到：`记录这次理发`；
- 有满意记录：`照上次再剪`；
- 处于洗后或第 7 天：`补一张真实状态`。

首页状态机遵守以下规则：

- 每个头发档案同一时间只能有一个“活动计划”；其他计划为草稿、已完成或已归档；
- 探索型计划需要 2～4 个候选；“照上次再剪”的复刻型计划允许只有 1 个候选；
- 若存在最近 14 天内应补的剪后记录，优先提示补记录；
- 其次提示已经接近理发日期的活动计划；
- 再其次提示继续完成最近更新的活动计划；
- 用户可以选择“稍后处理”，本次提示进入暂缓状态，不阻塞其他功能；
- 旧数据若出现多个活动计划，先让用户选择保留哪一个，不静默替用户关闭。

## 6.2 找方向

在真实 AI 上线前，将“试发型”改名为“找方向”，避免用户自然期待上传本人照片后立即得到生成结果。

包含：

- 编辑精选与授权示例；
- AI 合成示例；
- 用户上传参考图；
- 过去满意记录；
- 未来真实 AI 推荐和本人试剪。

不包含用户公开内容流。

## 6.3 档案

包含：

- 个人头发档案；
- 理发计划；
- 候选方案；
- 沟通卡；
- 理发记录；
- 标准发型；
- 避雷规则；
- 私人店铺与理发师备注；
- 发型时间轴。

## 6.4 我的

包含：

- 邮箱账号；
- 仅本机 / 已同步状态；
- 真实 AI 体验权益；
- 数据导出；
- 逐项删除和全部删除；
- 隐私说明；
- 关于产品；
- 理发师协作长期规划说明。

---

# 7. 新用户首次体验

## 7.1 首屏承诺

用户在 30 秒内应理解：

1. 剪前帮助做决定；
2. 剪时帮助说清楚；
3. 剪后帮助记住结果。

同时明确：

- 本地档案默认仅保存在当前设备；
- AI 合成示例不是根据用户照片生成；
- 只有用户主动使用未来真实 AI 时，才按单次任务说明上传边界；
- 外部分享只生成文件，不会自动发布。

## 7.2 最小建档

首次只要求：

- 直发 / 卷发 / 不确定；
- 发量少 / 中 / 多 / 不确定；
- 发丝细软 / 普通 / 粗硬 / 不确定；
- 当前大致长度；
- 每天愿意打理的时间；
- 最想解决的一个问题。

照片为可选，不阻断建档。

## 7.3 第一次成功体验

目标不是强迫用户填完整资料，而是让用户在十分钟内得到一张能带到理发店的沟通卡。

推荐路径：

```text
建立最小档案
→ 选择目标方向
→ 添加两个候选
→ 选主方案
→ 自动生成沟通卡
```

---

# 8. 邮箱账号、游客模式与数据归属

## 8.1 邮箱登录定位

邮箱账号回答“你是谁”，不回答“你买了什么”。账号和体验权益必须分开。

近期只支持：

- 页面内输入的六位邮箱验证码；
- 登录；
- 会话续期；
- 退出登录；
- 邮箱重新验证；
- 账号注销。

不在应用内保存用户密码。桌面浏览器以后可以增加 Magic Link，但微信内置浏览器不以 Magic Link 为主流程，避免链接跳到系统浏览器后丢失原浏览器里的 IndexedDB 档案。

安全契约：

- 邮箱验证码只保存带 Pepper 的 HMAC 哈希，不保存明文，也不进入日志；
- 登录成功后轮换会话标识，防止会话固定；
- 浏览器会话使用 `Secure + HttpOnly + SameSite=Lax` Cookie，不把长期令牌写入 `localStorage`；
- 所有修改账号、权益或云端数据的请求校验同源 `Origin`，并使用 CSRF Token 或等价双重防护；
- 注销账号和撤销全部会话前要求近期重新验证邮箱；
- 错误响应不泄露某个邮箱是否已注册。

## 8.2 游客模式

- 未登录用户可以使用所有本地免费功能；
- 游客档案不自动上传；
- 登录后不静默迁移或删除本地资料；
- 邮箱登录第一阶段只建立账号、会话和权益归属，不自动提供跨设备同步；
- 以后启用同步时，用户必须明确选择哪些结构化数据需要同步；
- 照片云备份属于更远期独立能力，默认关闭。

## 8.3 邮箱身份与同步分阶段

**邮箱身份 M2A** 只包含：

- 邮箱验证；
- 登录会话；
- 权益绑定；
- 退出登录；
- 撤销所有会话；
- 账号注销。

**结构化同步 M2B** 是独立的长期里程碑，不与邮箱登录同时实施。只有用户已经证明有跨设备恢复需求后才启动。

同步能力未来分为三层：

建议分为三层：

1. **仅本机**：照片和结构化档案都只在当前浏览器；
2. **结构化恢复**：同步档案字段、计划文本、沟通卡和规则，不同步原始照片；
3. **照片备份**：以后独立选择，单独告知保存位置、期限和删除方式。

界面必须始终显示当前数据层级，不能让用户误以为登录就已经备份全部内容。

## 8.4 后续登录方式触发条件

微信和手机号登录只在以下条件同时成立后进入实施评估：

- 邮箱登录和账号恢复已经稳定；
- 正式商业化上线已立项；
- 国内云迁移方案明确；
- 用户调研证明邮箱登录确实造成明显流失。

当前界面不显示不可用的微信或手机号灰色按钮。

---

# 9. 外部兑换码与 N 次体验权益

## 9.1 基本原则

应用不处理支付和订单，但真实 AI 会产生资源成本，因此需要一个最小权益边界。

```text
外部平台完成购买和发码
→ 用户在应用内完成邮箱登录
→ 用户输入独立兑换码
→ 应用后端向 VoucherProvider 校验
→ 权益绑定账号
→ 用户获得 N 次指定 AI 能力
```

外部平台可以更换，应用只依赖统一接口。

## 9.2 不采用共享密码实现 N 次权益

以下方案明确不采用：

```text
任意外部兑换码
→ AI 或算法转换
→ 所有人得到同一个固定密码
```

原因：

- 应用无法区分不同用户；
- 无法知道每个码还剩几次；
- 无法逐码停用；
- 固定密码可以被无限转发；
- AI 不应参与身份验证；
- 这种转换不会改变托管服务的实际使用性质。

共享密码仅允许用于免费、封闭、限时测试，并且不得向用户展示虚假的“剩余 N 次”。

## 9.3 推荐 VoucherProvider 边界

```ts
interface VoucherProvider {
  redeem(input: {
    code: string
    accountId: string
    idempotencyKey: string
  }): Promise<{
    redemptionId: string
    capabilities: ExperienceCapability[]
    totalUses: number
    expiresAt: string | null
  }>
}
```

首选由外部发码服务提供权威校验 API。外部服务是“这个码是否可兑换、可兑换多少次”的权威源；兑换成功后，应用自己的权益账本是“当前还剩多少次、每次任务是否已扣除”的唯一权威源。两边不能同时维护可变的 `remainingUses`。

如果外部服务没有校验 API，可由应用离线生成高熵独立码，仅将码的 HMAC 哈希和次数通过受控脚本导入；明文码交给外部工具发送。该方案不需要建设管理后台。

如果外部服务既不能校验，也不能发送应用生成的独立码，则只能继续使用共享测试口令，无法提供可靠的 N 次权益。

M4 是有条件里程碑：只有外部发码方提供可靠校验 API，或允许发送应用生成的独立码，才可以对外承诺 N 次权益。条件不满足时继续停留在免费封闭测试，不用共享密码冒充独立次数。

## 9.4 权益账本不变量

兑换和扣次必须满足：

- 同一兑换码指纹只能成功兑换一次；
- 同一外部 `redemptionId` 只能生成一个权益；
- 同一个 `idempotencyKey` 只能对应一个 AI 操作；
- `remainingUses` 永远大于等于 0；
- 同一码被两个账号并发兑换时，只允许最先提交的事务成功；
- 同一账号可以叠加多个码，扣次顺序固定为“最早过期优先，其次最早创建优先”；
- 外部码在兑换前是否过期由 VoucherProvider 判断；兑换后权益使用应用账本里的 `expiresAt`；
- 账号注销前若仍有剩余次数，必须二次确认；确认注销后权益全部撤销且不可恢复；
- 预占操作带租约到期时间，不能永久占住次数；
- 进程崩溃后，清理任务根据 AI 任务结果进行对账：已有合格结果则确认扣次，没有结果则释放；
- 自动质检认定为技术异常的结果不扣次数；仅因审美不喜欢不能自助退次，界面必须提前说明 AI 结果不保证符合个人审美。

## 9.5 一次体验的定义

> **一次真实 AI 体验 = 使用同一张输入照片和同一个发型方向，成功生成最多 3 个可比较候选的一次批次。**

统一扣次口径：

- 档案规则和固定素材给出的文字推荐不扣次；
- 一个成功生成批次扣 1 次；
- 同一批次的系统自动质检和技术重试不另扣；
- 用户主动更换发型方向并重新生成，扣 1 次；
- 用户主动微调并要求生成新图片，操作前确认并扣 1 次；
- AI 目标与真实结果复盘在第一版不计入生成次数，也不承诺由兑换码覆盖；以后若产生明显模型成本，作为独立 `ai_recap` 权益另行定义。

批次交付标准：

- 目标产物是 3 个通过质检的候选；
- 至少有 2 个通过质检，才自动判定为成功批次并扣 1 次；
- 只有 1 个通过时，系统在同一批次内最多自动补生成 2 次，不另扣；
- 补生成后仍只有 1 个时，让用户选择“接受这 1 个并扣 1 次”或“放弃并退回本次机会”；
- 0 个通过时直接判定失败并释放预占；
- 用户接受单张结果前必须看到候选数量不足说明，不能默认替用户确认。

以下行为不扣次数：

- 打开应用；
- 登录；
- 浏览固定示例；
- 建档；
- 编辑计划；
- 导出沟通卡；
- 使用本地隐私遮罩；
- 输入校验失败；
- 网络失败；
- 服务超时且未得到结果；
- 同一任务的幂等重试。

扣次流程：

```text
检查剩余次数
→ 原子预占 1 次
→ 执行 AI 任务
→ 结果质检通过
→ 正式确认扣除

若失败 / 超时 / 质检拒绝
→ 自动释放预占次数
```

## 9.6 用户看到的权益信息

“我的—体验权益”只显示：

- 可使用的能力；
- 剩余次数；
- 有效期；
- 最近一次使用结果；
- 输入新兑换码。

不显示订单、价格、支付状态或会员等级。

---

# 10. 个人头脸与头发档案

## 10.1 基础资料

- 昵称；
- 性别呈现（可选）；
- 年龄段（可选）；
- 当前长度；
- 最近一次理发时间；
- 染发史；
- 烫发史；
- 日常定型产品习惯；
- 每天可接受打理时间。

## 10.2 发质资料

- 直 / 卷程度；
- 发丝粗细；
- 发量；
- 是否易出油；
- 是否易炸毛；
- 是否易贴头皮；
- 是否有明显发旋；
- 是否有发际线困扰；
- 是否接受烫发；
- 是否接受染发。

所有专业字段允许选择“不知道”。

## 10.3 标准照片

按需求逐步补充：

- 正面；
- 左侧；
- 右侧；
- 后脑；
- 左前 45°；
- 右前 45°。

照片检查包括：

- 是否拍全头顶；
- 人脸和头发尺寸是否合适；
- 光线是否过暗；
- 是否戴帽；
- 是否有明显美颜；
- 是否存在多人；
- 头发是否被严重遮挡或扎起；
- 侧脸角度是否足够。

照片检查优先在本地完成。

## 10.4 AI 头脸与头发视觉分析

这是长期私人智能能力，不依赖社区。只有用户主动使用并确认上传边界后，才分析：

- 脸部长宽比例和轮廓；
- 额头暴露比例；
- 发际线位置与形态；
- 颅顶视觉比例；
- 左右视觉差异；
- 当前刘海覆盖；
- 当前长度、发量和贴头皮状态；
- 耳朵、眼镜和发型之间的关系。

分析只为推荐、可剪性和沟通卡服务，不给用户贴简单的“圆脸 / 方脸决定论”标签，不做身份识别或医疗判断。结果属于用户私人档案，默认不公开、不进入分享文件。

---

# 11. 找方向与参考图导入

## 11.1 内容来源

应用内可展示：

- 编辑精选；
- 明确授权的参考素材；
- AI 合成人物示例；
- 产品自有演示素材；
- 用户本人上传的参考图；
- 用户自己的过去理发记录。

不展示其他用户上传的公开内容。

## 11.2 目标方向

用户先选择想解决的问题，而不是面对大量专业筛选项：

- 看起来更精神；
- 修饰脸型；
- 少打理；
- 两侧不要太短；
- 顶部不要太蓬；
- 遮一点发际线；
- 适合戴眼镜；
- 留长过渡；
- 保守复刻；
- 想尝试明显变化。

## 11.3 参考图导入

支持用户导入：

- 外部平台截图；
- 理发店样片；
- 朋友提供的参考；
- 自己过去满意照片；
- 自己保存的 AI 图；
- 设备中的任意合法参考图。

应用只将其作为用户私人资料，不形成公开素材库。

## 11.4 发型拆解

未来 AI 可以把参考图拆为：

- 刘海；
- 顶部；
- 两侧；
- 鬓角；
- 后脑；
- 层次；
- 卷度；
- 纹理；
- 发色；
- 日常造型要求。

用户可以只采用其中某些元素。

## 11.5 私人自然语言搜索

自然语言搜索只查询：

- 编辑精选和授权素材；
- AI 合成示例；
- 用户自己的参考图标签；
- 用户自己的过去记录和标准发型。

示例：

- “不用烫、每天五分钟能打理的短发”；
- “两侧不要推太高”；
- “类似上次满意版本，但刘海再长一点”；
- “适合戴眼镜、顶部不要太蓬”；
- “从我的历史里找评分最高的三个方案”。

它不搜索其他用户资料，也不需要公共内容流。

---

# 12. AI 推荐与私人理发顾问

## 12.1 推荐输入

AI 综合：

- 用户头发档案；
- 当前长度；
- 用户手动描述的现实问题；
- 打理时间；
- 是否接受烫染；
- 过去满意和失败记录；
- 活动避雷规则；
- 本次场景和目标方向。

## 12.2 推荐输出

第一版只给三个方向：

1. **最稳妥**：现实可执行性最高、翻车风险最低；
2. **最适合当前目标**：综合外观、习惯和历史反馈；
3. **最值得尝试**：变化更明显，但会诚实说明风险。

每个推荐必须解释：

- 为什么适合；
- 需要保留哪些长度；
- 是否需要烫染；
- 每天大致打理成本；
- 最大风险；
- 需要理发师特别注意什么。

## 12.3 顾问边界

AI 顾问只使用用户自己的档案和用户主动提供的资料，不接入真人咨询，不建立问答社区，不输出医疗诊断。

---

# 13. 本人试剪与生成

## 13.1 第一版范围

真实 AI 第一版只实现：

- 一张合格正面照片；
- 三个明确方向；
- 每个任务返回一组可比较结果；
- 保留身份、五官、肤色、年龄感和眼镜；
- 只改变头发相关区域；
- 清楚区分用户原图、AI 结果和参考图。

不在第一版同时实现多视角、发色、烫发、胡须和 AR。

## 13.2 生成原则

- 决策模式优先保持真实光照和皮肤状态；
- 不默认美颜；
- 不改变脸型来制造“更适合”的假象；
- 不把摄影和吹风效果包装成纯剪裁结果；
- AI 结果必须带显式“AI 生成”标识；
- 明确说明不保证真实理发后完全一致。

## 13.3 后续多视角

只有在单视角通过第 31.4 节的授权样本和异常拦截验收后，才逐步增加：

- 左侧；
- 右侧；
- 后脑；
- 左右后 45°；
- 同方案跨角度一致性。

---

# 14. 发型微调与 AI 质量保障

## 14.1 微调动作

- 刘海更长 / 更短 / 更薄；
- 两侧更短但不要推太高；
- 顶部更蓬 / 更低；
- 后脑更饱满；
- 保留鬓角；
- 减少打薄；
- 卷度增加 / 减少；
- 更自然；
- 更接近日常未造型状态。

## 14.2 质量检查

生成后检查：

- 是否仍像本人；
- 五官是否被误改；
- 头发是否穿过眼镜；
- 刘海和发际线是否异常；
- 背景是否被大幅修改；
- 是否出现多人或多余肢体；
- 是否出现奇怪文字或水印；
- 不同视角是否保持同一发型；
- 是否满足用户明确的“绝对不要”。

明显失败的结果不计入一次成功体验。

---

# 15. 候选池与私人比较

探索型计划保存 2～4 个候选；从标准发型创建的复刻型计划允许只有 1 个候选。候选来源可以是：

- 用户参考图；
- 过去真实记录；
- AI 生成结果；
- 产品固定示例。

每个候选统一展示：

- 来源；
- 为什么可能适合；
- 现实可剪性；
- 当前长度是否足够；
- 是否需要留长、烫或染；
- 每天打理成本；
- 最大风险；
- 主方案 / 备选方案状态。

在真实 AI 可剪性上线前，上述字段必须标明来源：

- `用户填写`；
- `编辑素材说明`；
- `本地规则判断`；
- `尚未评估`。

不得继续使用无法验证的固定“AI 判断”文案冒充个性化结论。M6 上线后，才增加明确标记为 `AI 评估` 的结果。

每张导入图片还要记录两个彼此独立、默认关闭且可撤销的权限：

- `aiProcessingAllowed`：是否允许用于远端 AI 任务，并记录确认时间；
- `externalShareAllowed`：是否允许进入外部分享文件，并记录确认时间。

两项都关闭时，派生状态为“仅私人查看”。同一张本人照片可以同时开启两项权限。导入的外部截图和他人参考图默认两项都关闭，不会自动进入 AI 或分享流程。

不提供站内好友投票。用户若想征求意见，可从“分享文件工作室”导出静态对比图。

---

# 16. 现实可剪性与理发师沟通卡

## 16.1 现实可剪性

系统需要判断：

- 当前长度是否足够；
- 哪些区域需要继续留长；
- 大致需要留多久；
- 是否需要烫染；
- 是否依赖吹风或定型产品；
- 不打理时可能是什么状态；
- 哪些细节高度依赖理发师技术；
- 哪些效果主要来自摄影或造型。

## 16.2 沟通卡内容

图片：

- 主目标图；
- 备选目标图；
- 原始状态；
- 重点区域局部图；
- 后续阶段增加多视角目标。

文字：

- 整体要求；
- 顶部；
- 刘海；
- 两侧；
- 鬓角；
- 后脑；
- 最在意三项；
- 绝对不要三项；
- 来自历史记录的活动避雷规则。

沟通卡先自动预填，再允许用户逐项修改。

---

# 17. 理发现场模式

理发现场模式只服务用户把手机递给理发师查看，不是理发师端。

要求：

- 大图；
- 大字；
- 无底部导航；
- 无账号、权益或隐私设置；
- 十秒内看清重点；
- 在线成功打开一次后，支持离线重新打开同一张沟通卡；
- 页面重开后仍保留；
- 屏幕常亮能力受浏览器限制时给出说明；
- 支持局部放大；
- 支持打印和本地图片导出。

离线方案：

- Service Worker 缓存版本化应用壳、沟通卡路由所需静态资源和字体；
- 沟通卡文字、顺序和本地图片 Blob 保存在 IndexedDB；
- 应用更新时先下载新缓存，当前打开的沟通卡继续使用旧版本，用户主动刷新后再切换；
- 不把用户照片放入共享 HTTP 缓存；
- 浏览器不支持或清理离线缓存时，以预先导出的沟通卡图片作为兜底；
- 界面显示“已可离线查看”或“请先保存图片备用”，不作无法验证的离线承诺。

---

# 18. 理发过程与剪后档案

## 18.1 照片阶段

- 剪前；
- 过程；
- 剪完未造型；
- 已造型；
- 第一次洗头后；
- 第 7 天。

## 18.2 最小记录

剪后 60 秒内完成：

- 一张照片；
- 1～5 分满意度；
- 复刻或避雷；
- 一句备注（可选）。

## 18.3 完整记录

以后按需补充：

- 日期；
- 城市；
- 店铺；
- 理发师；
- 服务项目；
- 价格；
- 耗时；
- 是否推销；
- 是否办卡；
- 目标方案快照；
- 沟通卡快照；
- 实际打理时间；
- 洗后与第 7 天感受。

---

# 19. AI 目标与真实结果复盘

当真实 AI 方案和真实理发照片都存在时，系统可比较：

- 刘海是否过短；
- 两侧是否推高；
- 顶部是否打薄过度；
- 后脑是否缺乏饱满度；
- 层次是否接近；
- 差异来自剪裁、吹风还是发质；
- 哪一句沟通最有效；
- 下次最应该改变什么。

输出保持四部分：

1. 这次做得好的地方；
2. 与目标偏差最大的地方；
3. 下次应该怎样说；
4. 是否值得复刻。

AI 复盘只辅助用户总结，不公开评价具体理发师，也不形成公共排名。

---

# 20. 满意复刻、标准发型与避雷规则

## 20.1 标准发型

成功记录可沉淀为“我的标准发型”，保存：

- 最佳真实照片；
- 当时目标；
- 沟通卡；
- 店铺与理发师私人备注；
- 大致长度；
- 实际价格；
- 剪后第几天最好看；
- 日常打理方式。

下一次支持：

> 照上次满意版本再剪一次。

## 20.2 避雷规则

失败不能只记录低分，要沉淀具体规则：

- 两侧不要推太高；
- 顶部不要打薄过度；
- 刘海至少保留到什么位置；
- 后脑不要剪平；
- 不接受高渐变；
- 不接受强制办卡或价格不透明。

活动避雷规则自动进入下一张沟通卡，但用户可以关闭单条规则。

---

# 21. 私人店铺与理发师备注

店铺和理发师只作为用户私人档案字段。

可保存：

- 名称或昵称；
- 地址或大致位置；
- 联系方式；
- 用户自己的价格记录；
- 擅长与不擅长的私人备注；
- 是否推销；
- 是否办卡；
- 历史理发记录；
- 用户自己的满意度。

不提供：

- 公共评分；
- 他人评价；
- 排行榜；
- 商家认领；
- 公开作品；
- 在线预约；
- 能力匹配推荐。

## 21.1 私人聚合洞察

应用可以只基于用户自己的历史，计算：

- 去过多少次；
- 平均满意度；
- 最高和最低满意记录；
- 常见项目和耗时；
- 哪些发型曾被成功复刻；
- 用户自己的理发师稳定度排序。

这些结果只对本人可见，不上传成公共评分，不和其他用户比较，也不用于理发师匹配或商家曝光。

---

# 22. 发型记忆、生命周期与时间轴

产品长期记住：

- 用户喜欢和不喜欢什么；
- 最在意哪个区域；
- 哪个长度满意度最高；
- 哪次理发评分最高；
- 哪些规则反复导致翻车；
- 剪后第几天最好看；
- 平均多久理发一次；
- 哪种方案最容易复刻。

时间轴展示：

```text
计划
→ 目标方案
→ 沟通卡
→ 刚剪完
→ 洗后
→ 第 7 天
→ 满意度
→ 复刻 / 避雷
```

提醒优先做本地提示，不依赖复杂推送系统。邮箱提醒只有在用户主动开启且邮箱能力稳定后再增加。

年度报告只基于用户自己的私人数据，不做榜单或社交比较。

---

# 23. 隐私遮罩与分享文件工作室

“分享文件工作室”不是第五个底部导航，也不是独立内容广场。它是以下页面里的上下文操作：

- 候选比较页：导出 A/B/C/D 对比图；
- 沟通卡页：导出沟通卡或打印版；
- 理发记录页：导出多阶段复盘图；
- “我的—隐私工具”：单独打开遮罩编辑器。

## 23.1 分享原则

- 分享只生成本地文件；
- 不创建应用内帖子；
- 不创建公开投票链接；
- 不上传分享文件到应用服务器；
- 不追踪用户把文件发到哪里；
- 不抓取外部评论或互动结果。

## 23.2 文件类型

第一阶段提供：

1. A/B/C/D 候选对比图；
2. 隐私遮罩版征求意见图；
3. 理发师沟通卡；
4. 剪前 / 当天 / 洗后 / 第 7 天复盘图。

后续提供：

- 标准发型复刻卡；
- 避雷总结卡；
- 个人发型时间轴；
- 年度发型报告。

## 23.3 文件格式

- 3:4 竖版；
- 9:16 竖版；
- 普通单图；
- A4 打印版；
- WebP / JPEG / PNG。

提供：

- 保存图片；
- 系统分享；
- 复制提问文案；
- 不支持系统分享时的下载或长按保存指引。

导出前增加“分享检查”：

- 只允许 `externalShareAllowed = true` 的图片进入文件；
- 私人导入素材默认不能分享，用户需要逐张确认自己有权对外使用；
- 默认排除内部备注、价格、联系方式、店铺地址和邮箱；
- 检查截图中的用户名、头像、通知栏、二维码和联系方式；
- 提供裁剪、涂抹或遮挡工具处理截图来源信息；
- 检测并提醒背景人物，无法可靠脱敏时阻止导出；
- 不自动去除素材权利人已有的水印，也不鼓励移除来源标识。

## 23.4 隐私遮罩

- 自动定位在本地 Worker 中完成；
- 无人脸或模型失败可手动调整；
- 多人照片默认阻止导出；
- 支持拖动、缩放、旋转和样式切换；
- 导出前必须扁平化；
- 原图、关键点和编辑图层不上传；
- 提醒用户遮罩不等于绝对匿名。

## 23.5 AI 内容标识

包含 AI 生成图的分享文件必须有清晰可见的“AI 生成示意”标识，不把固定示例或生成结果伪装为真实理发结果。

---

# 24. 隐私、授权、删除与数据导出

## 24.1 默认本地优先

- 本地档案和照片默认只在当前设备；
- 登录不等于自动同步；
- 邮箱账号、权益数据与照片存储分层；
- 每次远端上传都需要明确目的。

## 24.2 真实 AI 上传边界

未来真实 AI 需要远端处理时，在任务开始前明确说明：

- 哪张图片会上传；
- 用于什么任务；
- 由哪类服务处理；
- 保存多久；
- 是否用于训练；
- 怎样删除；
- 失败后是否保留；
- 本次会消耗几次权益。

不允许在用户浏览固定示例或本地档案时后台上传照片。

第一版远端处理契约写死为：

- AI 输入原图使用短期对象保存，任务成功、失败或取消后 24 小时内从应用控制的存储删除；
- 供应商必须提供不用于训练的配置或合同承诺；无法关闭训练的供应商不进入生产；
- 供应商侧原图保留时间不得超过其完成任务所需的最短期限，并在接入文档中记录实际上限；
- 生成结果默认在云端保留 7 天用于断线恢复，随后删除；用户明确选择“保存到账号”时可延长到生成后 90 天，界面显示到期日；第一版不自动续期，用户取消保存或删除任务时立即进入删除流程，长期云备份留给独立 M2B；
- 仅包含请求 ID、错误码和任务状态的技术日志最多保留 14 天，不记录图片、明文码、完整邮箱或提示词中的敏感信息；
- 账号注销或用户删除任务时，应用立即删除自己的对象，并调用供应商删除能力；删除回执或失败重试状态必须可验证；
- 涉及跨境处理时，在任务开始前展示接收方类型、地区、用途、数据种类和保存期限，并取得所需同意。

## 24.3 用户控制

用户可以：

- 查看本机保存的数据类型；
- 删除单张照片；
- 删除单次计划；
- 删除单次 AI 任务及结果；
- 删除理发记录；
- 删除全部本地档案；
- 导出结构化档案；
- 导出照片清单；
- 注销邮箱账号；
- 删除已同步的数据；
- 选择照片是否参与云端备份。

## 24.4 照片授权

- 仅限 18 岁以上用户；
- 本地私人参考图可以是用户合法持有的发型素材，但默认只能私人查看；
- 用于账号头像、本人试剪或远端 AI 输入的照片，必须是用户本人或明确获得授权的成年照片；
- 进入外部分享文件的素材必须有对外分享权利，并通过逐张确认；
- 不通过身份证或人脸识别做年龄核验；
- 采用清晰的 18+ 和本人 / 授权确认；
- 不承诺遮罩后无法被熟人识别。

---

# 25. 核心数据模型

## 25.1 本地核心实体

```text
HairProfile
HeadProfile
LocalPhoto
HaircutPlan
Candidate
BarberBrief
HaircutRecord
HaircutPhoto
StandardStyle
AvoidRule
PrivateSalonNote
PrivateBarberNote
ShareExportPreset
LocalSyncState
```

`LocalPhoto` 和候选引用必须分别保存 `aiProcessingAllowed`、`aiProcessingConfirmedAt`、`externalShareAllowed`、`externalShareConfirmedAt`。两项默认均为 `false`，用户可独立开启或撤销。

## 25.2 云端按阶段实体

邮箱身份和真实 AI 权益阶段仅需要：

```text
Account
EmailIdentity
AuthSession
EntitlementGrant
VoucherRedemption
ExperienceOperation
AiTask
GeneratedAsset
```

`SyncManifest` 只属于长期 M2B 结构化同步，不随邮箱登录 M2A 一起建设。

## 25.3 明确不进入当前数据模型

```text
Post
Comment
Like
Follow
Message
PublicUserProfile
Vote
Poll
PublicCase
PublicSalon
PublicBarber
Order
Payment
Membership
PointAccount
PointLedger
Coupon
SupportTicket
BusinessDashboard
```

## 25.4 权益实体建议

```ts
interface EntitlementGrant {
  id: string
  accountId: string
  provider: 'external_api' | 'batch_import'
  providerRefHash: string
  capabilities: ExperienceCapability[]
  totalUses: number
  remainingUses: number
  status: 'active' | 'exhausted' | 'expired' | 'revoked'
  expiresAt: string | null
}

interface ExperienceOperation {
  id: string
  grantId: string
  accountId: string
  capability: ExperienceCapability
  idempotencyKey: string
  state: 'reserved' | 'committed' | 'released' | 'expired'
  reservedAt: string
  reservationExpiresAt: string
  committedAt: string | null
  providerTaskId: string | null
  resultAssetId: string | null
}
```

数据库约束至少包括：

- `voucher_redemptions(voucher_fingerprint)` 唯一；
- `voucher_redemptions(provider_redemption_id)` 唯一；
- `experience_operations(account_id, idempotency_key)` 唯一；
- `remaining_uses >= 0`；
- 兑换、预占和正式扣除均在事务或等价的原子边界中完成；
- 清理任务可重复执行，不得重复释放或重复确认同一操作。

---

# 26. 技术架构与服务边界

## 26.1 当前技术基础

```text
前端：Vue 3 + TypeScript + Vite
本地数据：Dexie + IndexedDB
本地图片：Canvas 重绘、方向纠正、压缩、EXIF 清除
本地遮罩：MediaPipe Face Landmarker + Web Worker
离线沟通卡：Service Worker 应用壳 + IndexedDB 卡片快照
测试期部署：Vercel H5 / Functions
代码仓库：GitHub 私有仓库
```

## 26.2 核心边界

```text
ArchiveRepository
  负责本地档案、计划、沟通卡、记录和规则

AuthProvider
  负责邮箱身份验证和会话

VoucherProvider
  负责外部兑换码校验和权益发放

EntitlementService
  负责次数预占、确认、释放和恢复

ImageProvider
  负责真实 AI 生成，可替换不同供应商

ImageQualityService
  负责输入照片和生成结果质量检查

MaskEngine
  负责本地遮罩定位、编辑和扁平导出

ShareExportService
  负责完全本地的分享文件生成

SyncService
  仅在用户明确启用后同步指定数据
```

## 26.3 基础设施原则

- 前端、身份、权益、AI、存储互相解耦；
- 测试期可以使用低成本或免费服务；
- 服务商更换不能要求重写本地档案核心；
- 任何规模化或商业使用前，托管方案必须符合真实用途；
- 不以隐藏使用性质或规避服务条款作为技术目标；
- 迁移到国内云时优先保持 API 和数据契约稳定。

---

# 27. 弱网、失败与恢复体验

## 27.1 本地存储失败

- 明确提示存储不可用或空间不足；
- 不把保存失败显示成成功；
- 提供导出当前沟通卡或资料的应急路径；
- 提醒无痕模式和浏览器清理可能导致数据丢失。

## 27.2 图片失败

- 不支持格式：说明支持的格式；
- 图片过大：本地压缩后再判断；
- 解码失败：允许重新选择；
- 多人照片：阻止隐私遮罩分享；
- 自动定位失败：进入手动模式；
- 导出失败：保留编辑状态并允许重试。

## 27.3 邮箱失败

- 六位验证码 10 分钟失效且只能使用一次；
- 同一邮箱 60 秒内不能重复发送；
- 邮箱、IP 和设备会话设置短期尝试次数限制；
- 邮件延迟：显示倒计时和重发入口；
- 邮件无效：不泄露账号是否存在；
- 会话过期：重新验证后恢复权益；
- 用户可以撤销当前会话或全部其他会话；
- 微信内置浏览器优先在当前页面输入验证码，不依赖跳转到系统浏览器；
- 登录失败不影响本地档案访问。

## 27.4 兑换失败

- 同一码重复提交：返回原结果，不重复发放；
- 已被其他账号兑换：统一提示无效、已使用或已过期；
- 外部服务不可用：不猜测、不临时放行；
- 已兑换权益在外部服务短暂故障时仍可使用；
- 明文码不得进入日志。

## 27.5 AI 失败

- 任务先保存草稿和幂等键；
- 网络中断后可继续查询同一任务；
- 失败不扣次数；
- 质检拒绝自动释放次数；
- 用户离开页面后回来仍可查看成功结果；
- 不重复提交产生重复扣费；
- 清理任务处理过期预占前，先向供应商查询任务状态；
- 供应商已产生合格结果则确认扣次，未产生结果才释放；
- 原图、生成结果和技术日志按第 24 章的期限自动删除；
- 删除失败进入幂等重试，不向用户虚假显示“已经完全删除”。

---

# 28. 视觉、无障碍与设备体验

继续使用当前视觉方向：

- 黑、米白、暖灰、少量警示棕；
- 杂志式人物图和信息层级；
- 少按钮；
- 克制圆角；
- 不使用美容院粉色、科技蓝光或廉价换脸站风格。

最低设备要求：

- 360px、390px、430px 手机宽度；
- Android Chrome；
- iOS Safari；
- 微信内置浏览器；
- 桌面预览；
- 键盘操作；
- 字体放大；
- 减少动态效果；
- 触控目标至少 45px；
- 导出和下载有微信浏览器降级路径。

---

# 29. 分阶段开发路线

## M0：范围重置与产品诚实化

目标：让现有应用与本计划完全一致。

- 从导航和用户路径移除好友投票；
- 停止创建公开投票链接和站内短评；
- 将分享改成本地文件导出；
- 将“试发型”在真实 AI 前改名为“找方向”；
- 所有固定素材明确标注“AI 合成示例”；
- 更新隐私文案，区分本地档案、未来 AI 上传和本地分享文件；
- 理发师端只保留路线说明；
- 删除支付、社区、后台等无实现暗示。

验收：产品中不存在帖子、评论、投票、公开主页或公开分享链接。

## M1：完成私人理发闭环

目标：不依赖真实 AI，也能为用户带来可验证价值。

- 渐进式最小建档；
- 首页按状态只给一个下一步；
- 候选统一信息，并明确来自用户填写、编辑素材、本地规则或尚未评估；
- 对编辑精选、私人参考标签和个人历史提供本地自然语言搜索；
- 主方案和备选方案；
- 沟通卡自动带入档案和避雷规则；
- 理发现场模式；
- 沟通卡 Service Worker 离线缓存和导出图片兜底；
- 60 秒剪后记录；
- 标准发型一键复刻；
- 历史避雷规则自动继承；
- 基于用户自己的记录生成私人店铺和理发师次数、平均满意度与最佳记录。

验收：新用户十分钟内完成沟通卡；老用户两分钟内创建复刻计划；私人搜索不返回其他用户数据；店铺和理发师聚合数字与原始记录逐项一致且没有网络公开入口。

## M2A：邮箱身份与权益归属

目标：用最低成本建立稳定身份，不提前建设同步系统。

- 当前页面内输入六位邮箱验证码；
- 游客继续使用本地功能；
- 登录不自动上传照片；
- 会话续期、退出和撤销全部会话；
- 账号注销；
- 为后续兑换权益提供稳定 `accountId`；
- 明确显示“档案仍仅保存在当前设备”。

验收：邮箱登录、退出和过期恢复不改变本地档案；微信内置浏览器无需跳出当前浏览器完成登录。

## M3：分享文件工作室

目标：让用户安全地把结果带到外部平台。

- 候选对比图；
- 遮罩征求意见图；
- 沟通卡；
- 多阶段复盘图；
- 3:4、9:16、普通图片和打印版；
- 保存、系统分享、复制文案；
- iOS、Android、微信浏览器降级。

验收：外部分享不产生任何站内内容或公网用户页面，网络中无原图和关键点上传。

## M4：次数权益基础

目标：支持外部独立码兑换 N 次真实 AI 能力，不建设交易系统。

- VoucherProvider；
- 独立码兑换；
- 权益绑定邮箱账号；
- 次数和有效期；
- 预占、确认、释放；
- 幂等重试；
- 不做后台 UI；
- 外部 API 或受控脚本导入。

进入条件：外部服务提供权威校验 API，或允许发送应用生成的独立码。否则 M4 不上线。

验收：同一码并发只能兑换一次；多码按最早过期顺序扣除；同一任务不会重复扣次；失败任务不扣；过期预占可以对账恢复；剩余次数不会出现负数。

## M5：真实 AI 单视角体验

目标：从固定示例升级为真正属于用户的结果。

- 输入照片质检；
- AI 头脸与头发视觉分析，并为每个推断显示来源、置信度和用户纠正入口；
- 三个推荐方向；
- 同一照片和同一方向一次生成最多 3 个候选；
- 现实模式优先；
- AI 显式标识；
- 基础质量检查；
- 失败自动释放次数；
- 结果可进入候选和沟通卡。

验收：用户能清楚区分原图、参考图和 AI 结果；任何视觉分析都可被用户纠正且不会覆盖用户手动资料；固定示例永不消耗次数；30 张授权测试照片中至少 80% 的批次在 90 秒内得到至少 2 个合格候选，其余批次严格进入补生成、单张确认或退次流程。

## M6：现实可剪性与智能复盘

目标：让 AI 从“生成图”升级为“减少翻车”。

- 当前长度判断；
- 留长建议；
- 烫染和打理要求；
- 风险说明；
- AI 目标与真实结果复盘；
- 自动生成下次沟通建议；
- 复刻和避雷知识逐步积累。

验收：对同时存在目标和真实结果的适用记录，系统必须输出“最大偏差、下次可直接说的一句话、复刻或避雷建议”；用户可以一键把建议带入下一张沟通卡；至少 10 位真实理发用户中有 8 位确认建议比空白重写更有帮助。

## M7：可选同步与长期增强

结构化同步是独立的 M2B，只有人工内测明确出现跨设备恢复需求后才实施。照片云备份继续单独评估。

只有前述阶段通过明确验收后，再实施：

- 可选结构化档案同步；
- 多视角一致生成；
- 局部微调；
- 发色和烫发模拟；
- 个人生命周期提醒；
- 年度发型报告；
- 更完整的个人时间轴；
- 可替换多模型路由；
- 可选照片云备份；
- AR 快速预览。

---

# 30. 当前实现与新计划的衔接

## 30.1 已有真实基础

- Vue 3 + TypeScript + Vite H5；
- 首页和四个主导航；
- 本地档案、计划和候选；
- 用户参考图和过去记录来源；
- 图片方向纠正、压缩和 EXIF 清除；
- 沟通卡保存、打印和导出；
- 六阶段理发记录；
- 标准发型和避雷规则；
- MediaPipe 本地隐私遮罩；
- 响应式和浏览器测试基础。

## 30.2 当前部分实现

- 头发档案字段不完整；
- 首页状态引导仍可加强；
- 标准发型不能完整一键复刻；
- 避雷规则尚未完整自动进入沟通卡；
- 理发现场模式仍需专门强化；
- 分享文件类型不足；
- 邮箱账号尚未实现；结构化同步属于通过需求验证后再做的独立 M2B。

## 30.3 当前仅为演示

- AI 推荐；
- 本人试剪；
- 发型微调；
- 现实可剪性；
- 打理成本和推荐理由。

这些能力当前来自固定合成人物和预写文案，必须继续诚实标注。

## 30.4 按本计划退出的现有功能

- 公网投票创建；
- 公开投票链接；
- 好友投票；
- 匿名票数；
- 投票短评；
- 投票管理和撤销页面；
- 为投票上传遮罩图；
- 投票数据库和清理任务。

退出前应先确认是否存在测试数据，再按明确迁移步骤删除相关远端数据和 Blob，不能只隐藏入口。

---

# 31. 发布与验收标准

每个面向用户的阶段至少邀请 10 位符合目标年龄的测试者完成无引导任务，并按 P80 判断：至少 8 人能在目标时间内独立完成。当前不建设行为埋点，完成时间、失败点和主观反馈由人工测试记录。

## 31.1 私人闭环

- P80 新用户从“开始准备下次理发”到完成一张可用沟通卡不超过十分钟；
- 可用沟通卡必须含目标图、整体要求、顶部、刘海、两侧、后脑、最在意项和绝对不要项；
- P80 剪后最小记录从打开表单到保存不超过 60 秒；
- P80 有标准发型的用户从首页到创建复刻计划不超过两分钟；
- 刷新和浏览器重开后本地资料仍在；
- 删除档案不会留下不可发现的孤立数据。

## 31.2 分享文件

- 应用内无帖子、评论、投票和公开主页；
- 所有分享产物在本地扁平化；
- 原图、关键点和编辑图层不上传；
- iOS、Android 和微信浏览器至少存在一种可靠保存路径；
- AI 图有显式标识；
- 分享文件不包含跟踪参数。

## 31.3 邮箱与长期同步

- 未登录仍能使用本地免费功能；
- 登录失败不影响本地档案；
- M2A 上线时明确显示“档案仅在本机”，不暗示已经同步；
- M2B 上线后用户能区分仅本机和已同步；
- 邮箱验证码符合有效期、单次使用、重发限制和防账号枚举要求；
- 微信内置浏览器可在同一浏览器完成登录；
- 安全测试确认验证码数据库中无明文、会话 Cookie 具备 `Secure + HttpOnly + SameSite`、登录后会话标识已轮换、跨站状态修改被拒绝；
- 注销后云端数据按说明删除；
- 照片不会因登录自动上传。

## 31.4 权益与 AI

- 独立码可以正确绑定账号；
- 同一码重复提交不重复发放；
- 并发任务不会把次数扣成负数；
- 失败、超时和质检拒绝不扣次数；
- 同一任务恢复不重复扣；
- 用户在操作前知道是否会扣次；
- 固定示例不扣次；
- 明文码和完整邮箱不进入日志；
- 至少使用 30 张获得授权的成年测试照片完成发布前质量样本，严重五官变形、多人、头发穿模和明显异常结果必须在展示给用户前被阻止；
- 每次生成的网络记录只出现用户明确确认的输入，不出现其他本地档案照片；
- 成功、失败、取消、任务删除和账号注销分别验证原图、结果和供应商副本的删除流程；
- 自动质检失败可自动重新生成或退次；单纯审美不喜欢不触发自助退次。

## 31.5 设备与无障碍

- 360 / 390 / 430px 无横向溢出；
- 桌面可正常预览；
- 键盘可操作；
- 字体放大后关键动作仍可见；
- 减少动态效果生效；
- 触控目标达到 45px；
- Android Chrome 与 iOS Safari 分别完成“在线打开沟通卡一次 → 关闭页面 → 断网 → 重新打开同一张卡”的实机验证；
- 微信内置浏览器若不能可靠离线重开，必须在在线阶段明确提示并完成“导出沟通卡 → 断网 → 从系统相册打开”的实机兜底验证；
- 微信内置浏览器有真实保存验证。

---

# 32. 成本控制原则

1. 能本地完成的图片处理不调用云服务。
2. 固定示例、本地档案、沟通卡、记录、遮罩和导出不消耗 AI 次数。
3. 真实 AI 只在用户明确发起时调用。
4. 每个任务先做图片质量检查，避免把明显不合格输入送给付费模型。
5. 技术失败和质检失败不向用户扣次。
6. 第一版只做单视角，暂不承担多视角、AR 和高级发色成本。
7. 不建设社区、内容审核、支付和运营后台，减少长期维护成本。
8. 测试期优先低成本方案，但不以规避服务条款为设计目标。
9. 不采集全站行为轨迹；迁移判断只使用 AI 权益账本中的任务总量、托管资源用量、人工大陆可达性测试和服务商额度。当任一关键免费额度连续 7 天超过 70%，或人工测试无法稳定访问时，启动迁移评估。
10. 迁移前保持 AuthProvider、VoucherProvider、ImageProvider 和存储边界可替换。

---

# 33. 冻结功能停车场

以下能力不进入当前阶段，但保留重新评估条件。

## 33.1 微信和手机号登录

触发条件：正式商业化立项、至少 20 位目标用户测试中有 5 位以上因邮箱流程无法继续、国内云方案确定。

## 33.2 支付、订单、会员和积分

触发条件：外部兑换机制已经验证真实付费需求，且应用内交易能显著改善用户体验。未达到条件前不建表、不做占位页面。

## 33.3 理发师或商家端

触发条件：至少 20 位真实使用沟通卡的用户访谈中，有 10 位以上持续提出同一种理发师协作需求，并且团队有能力承担身份、内容和服务治理。

## 33.4 高级生成和 AR

触发条件：30 张授权测试照片的单视角质量门槛通过，并且至少 10 位真实理发用户确认单视角结果对候选决策和沟通卡有帮助。不依赖未建设的行为埋点。

## 33.5 经营分析

触发条件：需要系统化经营决策，而不是为了“看起来完整”。在此之前只保留匿名、最小化的技术故障信息。

---

# 34. 永久排除清单

以下能力不随用户规模重新评估，除非产品定位被正式重新定义：

- 应用内社区；
- 用户发帖；
- 评论、点赞、关注、私信；
- 公开用户主页；
- 好友投票和公开投票链接；
- 公开案例投稿；
- 公共理发师、店铺评分和排行；
- 陌生人社交；
- 外部平台评论抓取；
- 为增长而建设的公共内容流；
- 把固定 AI 示例伪装成用户本人结果；
- 用共享密码虚假表示每人独立 N 次权益；
- 为了外部分享而上传原图或建立站内公开页面；
- 以隐藏商业用途、规避托管条款作为技术目标。

---

# 35. 原全量方案章节迁移摘要

## 保留并强化

- 个人档案；
- AI 推荐、试剪和微调；
- 候选比较；
- 现实可剪性；
- 理发师沟通卡；
- 剪后档案；
- AI 目标与真实结果复盘；
- 满意复刻；
- 避雷档案；
- 隐私遮罩；
- 发型记忆、生命周期、提醒、时间轴和年度报告；
- AI 质检与多模型边界；
- 成熟首页和长期个人护城河。

## 重写

- 发型发现：改为编辑精选、授权素材和私人导入，不做 UGC 流；
- 朋友投票：改为本地对比图和外部提问文案；
- 店铺与理发师：改为纯私人备注；
- 分享传播：改为本地文件工作室；
- 用户账户：邮箱先行、游客优先、照片默认不上传；
- 商业体系：移出应用，改为外部兑换权益边界；
- 技术形态：按现有 H5、本地优先和可替换服务重写；
- 数据模型：删除社区、投票、订单、积分和公共商家实体；
- 核心指标：改为人工内测验收，不建设经营后台；
- 产品结构：只保留私人闭环。

## 移除

- 社区；
- 公开现实案例库；
- 好友投票；
- 公共理发师匹配；
- 到店前站内确认；
- 理发师和商家工作台；
- 内容审核后台；
- 应用内支付、订单、会员和积分。

## 长期冻结

- 微信和手机号登录；
- 多视角高级生成；
- 发色、烫发、长度滑杆；
- AR；
- 理发师协作；
- 应用内交易；
- 复杂经营分析。

---

# 36. 最终产品结构

```text
咋剪发
│
├─ 首页
│  ├─ 当前头发状态
│  ├─ 唯一下一步
│  ├─ 上次结果
│  └─ 照上次再剪
│
├─ 找方向
│  ├─ 编辑精选 / AI 合成示例
│  ├─ 私人参考图导入
│  ├─ 过去满意记录
│  ├─ 未来真实 AI 推荐
│  ├─ 本人试剪
│  ├─ 私人候选比较
│  └─ 导出候选对比图
│
├─ 档案
│  ├─ 个人头发档案
│  ├─ 理发计划
│  ├─ 沟通卡
│  ├─ 理发现场模式
│  ├─ 六阶段记录
│  ├─ 标准发型
│  ├─ 避雷规则
│  ├─ 私人店铺 / 理发师备注
│  ├─ 发型时间轴
│  ├─ 导出沟通卡
│  └─ 导出多阶段复盘图
│
└─ 我的
   ├─ 邮箱账号
   ├─ 数据所在位置
   ├─ 体验权益
   ├─ 本地隐私遮罩工具
   ├─ 数据导出与删除
   ├─ 隐私说明
   ├─ 关于产品
   └─ 理发师协作长期规划
```

分享文件工作室是候选、沟通卡、记录和隐私工具共用的底层导出能力，不是第五个底部导航或内容频道。

产品中没有社区、公开投票、公共主页、支付、订单、会员、积分、商家后台或理发师工作台。

---

# 37. 最终判断

本产品真正值得长期投入的不是社区规模，也不是把所有商业模块塞进应用，而是以下四个私人能力：

1. **把用户自己的历史变成下一次可直接使用的决策依据。**
2. **把参考图和 AI 图翻译成现实可执行的理发师沟通卡。**
3. **把真实结果、洗后状态和第 7 天状态沉淀为复刻与避雷经验。**
4. **在不建立站内社交的前提下，让用户安全地生成文件并自行去外部分享。**

推荐的产品演进顺序是：

```text
私人理发闭环
→ 邮箱身份
→ 外部分享文件
→ 可验证的次数权益
→ 真实 AI 单视角体验
→ 现实可剪性与智能复盘
→ 长期个人发型记忆
```

最终目标不变：

> **每次理发都不再从零开始；每一次成功和失败，都让下一次更不容易翻车。**
