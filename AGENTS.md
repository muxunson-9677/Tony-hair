# “咋剪发”项目执行规则

本文件适用于本仓库内的产品设计、需求拆解、开发、测试、评审与发布。任何新功能或页面改动都必须遵守以下规则。

## 1. 最大程度降低用户理解成本（核心规则）

产品应像优秀的 iPhone 应用一样，让普通用户不阅读说明也能快速理解当前状态、可执行动作和下一步结果。优先追求高效、便捷、准确的业务逻辑，不得让用户理解内部数据模型、开发术语或复杂流程。

### 产品与业务逻辑

- 每个页面只突出一个当前最重要的任务和一个明确的主操作；次要能力按需展开。
- 先识别用户此刻要完成的真实任务，再决定页面、字段和入口；不得把数据库结构直接翻译成表单。
- 能由已有数据推断、自动填写或提供可靠默认值的内容，不要求用户重复选择或输入。
- 同一目标只保留一条清晰主路径；避免重复入口、同义选项、无效占位和没有后续结果的按钮。
- 业务规则必须准确、一致且可预测。相同状态在首页、档案、计划、沟通卡和记录中不得给出相互矛盾的动作。
- 错误提示使用用户语言，说明发生了什么、数据是否保存以及下一步怎么办；不得暴露技术错误或让用户猜测。
- 危险操作需明确后果并防止误触；可恢复操作不增加不必要的确认步骤。

### 信息、文案与交互

- 优先使用用户熟悉的短句和生活语言；删除可有可无的标题、解释、专业术语和重复文案。
- 通过照片、当前结果和下一步行动表达个性化价值，不能只展示放在任何用户身上都成立的通用文字。
- 采用渐进披露：首次只显示完成当前任务所需的信息，高级选项在用户主动需要时再出现。
- 控件必须看起来可操作，并对按下、拖动、松开、完成、失败和禁用状态提供及时、克制且可中断的反馈。
- 动画只用于解释状态变化和确认操作，不得延迟任务、抢夺注意力或成为理解功能的前提；减少动态效果时必须提供等价体验。

### 移动端验收

- 以首次使用的普通用户和真实手机单手操作为默认场景，优先在 390×844 视口验证。
- 新用户无需教程，应能回答：我在哪里、这里能做什么、现在应该点什么、完成后会得到什么。
- 核心任务应使用尽可能少的页面、决策和输入完成；如果存在更少步骤且不降低准确性和安全性的方案，必须选择更简单的方案。
- 所有主要操作需具有清晰层级、足够触控面积、即时反馈，并在加载、空数据、失败、离线和返回后保持可理解。
- 不能以“测试通过”代替真实体验验收；交付前必须逐页检查真实移动端页面、完整主流程、错误状态、文字放大和触控反馈。

### 决策优先级

出现取舍时，按以下顺序决策：

1. 用户真实任务是否正确完成；
2. 用户是否无需学习即可理解；
3. 操作是否高效、便捷且少出错；
4. 信息与反馈是否清楚一致；
5. 视觉与动效是否帮助理解；
6. 开发实现是否方便。

不得为了展示技术能力、视觉效果、内部架构或历史实现，增加用户的理解成本和操作负担。
