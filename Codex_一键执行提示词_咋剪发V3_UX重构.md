# Codex 一键执行提示词｜咋剪发 V3 Apple级体验重构

你现在不是单纯的前端工程师。你需要同时承担：
- Principal Product Manager
- Senior Consumer UX Designer
- Interaction / Motion Designer
- Vue 3 Frontend Architect
- QA / Accessibility Reviewer

但不要“角色扮演式”输出空洞分析。所有判断必须落到当前仓库的真实页面、真实组件、真实状态和真实用户路径。

---

## 0. 项目与产品基线

项目：Tony-hair / “咋剪发”。

先读取并遵守仓库中的现有文档，尤其是：
- `发型产品_全量计划_v2_用户体验优先.md`
- 如果我把 `咋剪发_V3_Apple级用户体验重构方案.md` 放入仓库，也必须完整读取它。
- 所有现有 AGENTS.md / README / package / route / store / Dexie schema / tests。

产品定义：
> “咋剪发”是一套私人的理发决策、沟通、记录与长期记忆工具。
> 核心是：剪前帮用户做决定，剪时帮用户说清楚，剪后帮用户记住；每剪一次，下一次更不容易翻车。

当前范围只关注：
- M0
- M1
- M2A
- M3
- M5
- M6

强制范围锁：
- M4 已退出当前计划：不设计、不开发、不验收、不计入未完成。
- M7 冻结：除非我以后明确恢复，否则不提前建设。
- 不新增社区、公开投票、公开用户主页、支付、订单、会员、积分、商家端、理发师端、公共排行榜、运营后台。
- 不为了“产品完整”扩业务边界。

---

# 1. 本任务真正目标

不要继续把应用做成：
“页面 + 卡片 + 表单 + 按钮 + 信息展示”。

我要把它重构成：
> 一个几乎不用学习就能使用的私人理发助理 / 私人理发决策系统。

“Apple级体验”不是毛玻璃和圆角，而是：
1. 一个时刻只让用户做一个核心决定。
2. 先给价值和结果，再解释专业细节。
3. 同一个对象在流程中保持空间和视觉连续性。
4. 能直接点照片、拖动、比较，就不要先让用户学习术语。
5. 每次按下、拖拽、保存、撤销、失败都有即时反馈。
6. 动效用于解释状态和因果，不做装饰。
7. 系统内部可以复杂，用户表面必须简单。
8. 用户只需说人话；系统负责转成理发结构和长期记忆。

最重要的最终体验：
> 用户第二次回来时会明显感到“它居然记得我上次哪里剪坏了、哪里满意了”。

---

# 2. 先审计，不要立即改代码

第一阶段必须先完成 CURRENT PRODUCT AUDIT。

你必须真实读取代码并运行应用，不能只看计划文档推测。

审计至少包括：

## 2.1 当前信息架构
列出：
- 所有一级导航
- 所有二级页面
- 所有主要路由
- 所有核心用户任务
- 每个页面当前 Primary CTA
- 页面之间如何跳转

判断：
- 哪些页面是按“数据库/功能模块”组织，而不是按用户任务组织
- 哪些页面让用户理解内部概念
- 哪些入口重复
- 哪些页面没有唯一下一步

## 2.2 当前业务状态机
找到真实代码中：
- 用户档案状态
- 活动理发任务/计划
- 候选
- 主方案/备选
- 沟通卡
- 理发记录
- 标准发型
- 避雷规则
- 分享状态
- AI 占位/真实 AI 状态
- 登录占位状态

画出当前状态流。

检查：
- 是否存在死路
- 是否存在重复创建
- 是否存在多个活动任务冲突
- 刷新/返回后状态是否丢失
- 本地数据是否可能被 UI 重构破坏

## 2.3 逐屏 UX Audit
对每个关键页面从以下角度审查：
- 用户来到这里时脑中真正的问题是什么？
- 当前页面第一眼是否回答它？
- 页面是否有唯一主动作？
- 是否出现无价值信息？
- 是否有用户不懂的术语？
- 是否重复展示同一信息？
- 是否需要过多点击？
- 是否让用户做本可由系统推断的工作？
- empty/loading/error/offline 是否完整？
- 是否存在假 AI、假登录或误导性可用状态？
- 是否有明显视觉层级、触控、可访问性问题？

## 2.4 动效与交互 Audit
真实操作：
- 点卡片
- 返回
- 添加/删除候选
- 确定主方案
- 打开沟通卡
- 进入理发现场
- 保存剪后记录
- 复刻
- 避雷
- 分享/导出

找出：
- Route Push 过多造成的割裂
- 点击无反馈
- 列表突然跳动
- Modal/Sheet 行为不统一
- 保存只 Toast、不体现状态变化
- 没有 Undo
- 动画不可打断
- reduced-motion 缺失

## 2.5 实机/浏览器检查
至少检查：
- 360px
- 390px
- 430px
- desktop preview
- keyboard
- font scaling / long text
- prefers-reduced-motion

不要只跑测试，必须真正渲染页面。

---

# 3. 审计后先产出文件

在不改业务代码前，先生成：

`docs/UX_AUDIT_V3.md`

内容必须包括：
1. 当前产品体验地图
2. P0 / P1 / P2 问题清单
3. 每个问题的用户影响
4. 根因
5. 对应代码位置
6. 推荐改法
7. 是否涉及数据迁移
8. 是否涉及新增依赖
9. 风险

然后生成：

`docs/UX_V3_IMPLEMENTATION_PLAN.md`

要求按“用户闭环”而不是按“文件”拆任务。

除非发现会造成数据丢失、仓库状态不安全或存在无法继续的硬阻塞，否则不要停下来等我逐项确认；继续按下面的范围实施 P0。

---

# 4. V3 必须实现的产品结构

## 4.1 首页不再是功能目录

首页必须根据用户状态只突出一个主动作。

至少支持：

- 未建最小档案
  → `告诉我你的头发`

- 已建档但没有本次任务
  → `准备下次理发`

- 正在找方向
  → `继续选方向`

- 只有 1 个探索型方案
  → `再加一个比较`

- 已有 2–4 个方案
  → `选一个主方案`

- 已确定主方案
  → `生成“给理发师看”`

- 临近理发日
  → `打开给理发师看`

- 理发后待记录
  → `记一下这次结果`

- 有满意历史
  → `照上次再剪`

- 有失败历史
  → `准备这次理发`
  并自动带入相关避雷规则

首页不可同时铺满大量功能入口。

---

# 5. 用户主流程改为三个自然语言入口

在合适的“准备本次理发”入口提供：

## A. 帮我选
适合不知道怎么剪的人。

先问：
`这次你最想解决什么？`

不要先问专业发型名称。

然后按需要渐进询问：
- 每天愿意打理多久
- 是否接受烫/染
- 两侧是否有绝对限制
- 刘海是否有绝对限制
- 本次场景

结果最终只给三个有明确取舍的方向：
1. 最稳妥
2. 最符合这次目标
3. 最值得尝试

每个方向首屏只展示：
- 为什么
- 现实代价
- 最大风险

更多细节点击再展开。

## B. 我有参考图
用户导入自己合法持有的参考图。

目标不是“上传后存进图库”，而是：
> 让用户指着图表达自己喜欢什么。

如果技术/现有实现允许：
- 图上轻量标出 刘海 / 顶部 / 两侧 / 后脑
- 点击区域后打开底部 Sheet
- 问：“这里你喜欢什么？”
- 提供少量自然语言选项 + 自定义一句话

将结果转成结构化理发要求。

不要要求用户先理解专业术语。

## C. 照上次剪
如果存在满意历史：

首页直接展示上次满意结果。

主动作：
`照上次剪`

系统自动继承：
- 上次目标
- 沟通卡
- 活动避雷规则
- 私人店铺/理发师备注（如果适用）

只问：
`这次要不要改一点？`

允许用户直接说：
“刘海保持，两边长一点。”

只修改相关区域。

---

# 6. 术语降维

内部数据模型可以保留，但用户主流程不要强迫理解：

- HaircutPlan / 理发计划
- Candidate / 候选
- StandardStyle / 标准发型
- AvoidRule / 避雷规则
- HairProfile / 头发档案
- BarberBrief / 沟通卡

优先改为自然语言：

- “这次怎么剪”
- “我在考虑的方案”
- “上次满意的剪法”
- “下次一定别这样”
- “我的头发”
- “给理发师看”

注意：
不要为了改文案破坏数据模型。

---

# 7. 现实可剪性必须从“文字卡片”升级成“决策系统”

每个主方案必须回答：

1. 现在能不能剪
2. 哪些地方长度不够
3. 是否需要留长
4. 是否依赖烫/染
5. 每天大概需要多少打理
6. 不打理时可能是什么状态
7. 最大翻车风险
8. 哪些高度依赖理发师技术

第一视觉层只给：
- 总结论
- 1–3 个最大风险
- 下一步

不要一次铺满所有说明。

如果当前真实 AI 尚未上线：
- 明确标记来源
- 用户填写
- 编辑素材说明
- 本地规则
- 尚未评估

绝不能把固定文案冒充 AI 个性化结论。

---

# 8. 增加“理想效果 / 日常状态”体验框架

这是重要体验方向。

如果当前阶段没有真实模型能力，不允许造假。
可以先建立 UI / 数据契约 / 示例演示，但必须明确标记为示例。

未来真实 M5/M6 支持后：

同一方案允许快速切换：
- `理想效果`
- `日常没怎么打理`

通过 Crossfade / shared image transition 显示差异。

目的不是炫技，而是让用户知道：
> 参考图好看的一部分到底来自剪裁，还是来自吹风、烫发、造型、摄影。

---

# 9. 候选比较必须帮助“做决定”

探索型只保留 2–4 个方案。

比较维度优先限制为：
- 现实可剪性
- 每日打理成本
- 变化程度
- 最大风险

不要堆十几个标签。

允许：
- 设为主方案
- 设为备选
- 删除
- Undo

当用户确定主方案时：
不要让主图消失后重新进入陌生页面。

尽量保持同一图片对象自然扩大/移动，进入下一阶段。

---

# 10. 沟通卡升级为“给理发师看”

不要让用户感觉自己在维护一张后台表单。

自动生成：
- 主目标图
- 备选图（如果有）
- 顶部
- 刘海
- 两侧
- 鬓角
- 后脑
- 最在意 3 项
- 绝对不要 3 项
- 活动避雷规则

原则：
- 自动预填
- 用户只校正
- 用区域编辑 Sheet，而不是长表单

主 CTA：
- `到店打开`
- `保存图片备用`

---

# 11. 理发现场模式必须成为 Hero Feature

进入后：

完全隐藏：
- 底部导航
- 账号
- 设置
- 隐私
- 与当前理发无关的所有信息

首屏只保留：
- 主目标大图
- 最在意 3 项
- 绝对不要 3 项

再提供：
- 顶部
- 刘海
- 两侧
- 后脑

点击区域：
→ 局部放大
→ 一句话要求

支持：
- 大图
- 大字
- 高对比
- 局部放大
- 现场快速切换
- 已在线打开后的离线重开（若当前里程碑实施）
- 否则明确提醒保存图片作为离线兜底

目标验收：
> 一个完全不知道本产品的理发师，在 10 秒内可以说出用户最在意什么、绝对不要什么、目标是什么。

---

# 12. 剪后记录必须从“填写记录”变成“系统学习”

第一问题：
`下次还这么剪吗？`

三个选项：
- 就这样
- 有一点要改
- 别再这样

然后：
- 剪后照片
- 可选一句话
- 必要时区域选择

不要第一屏要求：
- 城市
- 店铺
- 价格
- 耗时
- 服务项目
- 复杂评分字段

这些可以以后补。

保存后不要只显示：
`保存成功`

必须明确告诉用户系统学到了什么，例如：
> “下次我会自动记住：两侧不要推过太阳穴。”

规则：

### 就这样
→ 建议保存为可复刻版本。

### 有一点要改
→ 把差异变成下一次区域调整。

### 别再这样
→ 建议生成具体避雷规则，用户确认。

---

# 13. 长期记忆必须真正影响下一次

系统不能只是“保存历史”。

下一次准备理发时应主动读取：
- 最近满意记录
- 当前有效避雷
- 用户最常在意区域
- 上一次明确调整
- 可复刻版本

例如首页：
> “上次你觉得两侧推得太高，这次已经帮你加入‘不要推过太阳穴’。”

这才是本产品的核心差异化。

---

# 14. 动效规范

不要“加一些高级动画”。

必须建立 motion tokens / 统一交互语言。

推荐：

### Pressed
80–140ms
- 卡片缩到约 0.98
- 阴影/亮度轻微变化

### Spring
220–420ms
- Sheet
- 卡片扩大
- 位置变化
- 可打断

### Shared element
260–480ms
适用于：
- 候选 → 详情
- 候选 → 主方案
- 主方案 → 给理发师看

### Crossfade
180–300ms
适用于：
- 理想效果 ↔ 日常状态
- 图片/解释状态切换

### Undo
删除、规则关闭、候选移除等非不可逆动作：
- 即时执行
- 2–4 秒 Undo

强制：
- 支持 prefers-reduced-motion
- reduced motion 下不要靠位移表达唯一含义
- 用户快速返回/连续操作时动画可中断
- 不要用 600ms+ 动画拖慢主流程

可以评估：
- Motion for Vue
- AutoAnimate
- Reka UI

但：
1. 先检查项目已有依赖。
2. 不重复引入等价库。
3. 新增依赖必须说明价值、bundle/维护风险。
4. 不为了动画重写整个应用。

---

# 15. UI 视觉硬规则

1. 照片是第一视觉内容。
2. 一屏最多一个 Primary CTA。
3. 毛玻璃只用于底部导航、悬浮操作条、短暂浮层。
4. 正文卡片保持白/浅灰、清晰边界和一致层级。
5. 蓝色 = 主操作/选中。
6. 绿色 = 已验证/可行。
7. 橙色 = 需要注意。
8. 红色 = 明确风险/绝对不要。
9. 颜色不能只做装饰。
10. 同类图片卡、按钮、标题、间距建立 token。
11. 点击目标 >= 45px。
12. 卡片正面不要堆 7–10 个标签。
13. 摘要 → 点击展开。
14. 不增加没有决策价值的数据或统计。
15. 不做美容院粉色模板、廉价霓虹 AI 工具风。

---

# 16. 渐进式披露

首次建档只收集真正必须信息：
- 直 / 卷 / 不确定
- 发量
- 发丝
- 当前长度
- 每日打理时间
- 最想解决的问题

以下信息在真正需要时再问：
- 染烫史
- 发际线
- 发旋
- 贴头皮
- 高级头型判断
- AI 置信度
- Provider
- 远端上传授权
- 完整店铺/价格/耗时

禁止“为了资料完整”提前要求填写。

---

# 17. 业务逻辑优先级

强制：

1. 用户明确“绝对不要” > AI 推荐。
2. 用户手动修正事实 > AI 推断。
3. 最近明确反馈 > 较旧历史。
4. 永久/活动避雷除非用户关闭，否则参与下一次。
5. 复刻型允许一个方案。
6. 探索型建议 2–4 个方案。
7. 现实不可剪时必须先给留长/替代路径。
8. 不知道就显示“尚未评估”。
9. 新方案与避雷冲突时必须提醒。
10. 不允许 AI 静默覆盖用户资料。

---

# 18. 数据与迁移安全

非常重要：

现有技术基础：
- Vue 3
- TypeScript
- Vite
- Dexie + IndexedDB
- 本地照片和相关本地处理

修改 UI 前先审计现有 schema 和 migration。

禁止：
- 删除现有用户数据
- reset IndexedDB 作为“修复”
- 为 UI 重构改主键/关系而不做迁移
- 静默关闭活动任务
- 清空旧兼容数据

如需 schema 变化：
先写 migration plan + tests。

---

# 19. Git 安全

在开始前检查：
- git status
- 当前 branch / worktree
- 未提交修改
- 当前基线

禁止：
- `git reset --hard`
- `git clean -fd`
- 覆盖用户未提交成果
- 删除不属于本任务的文件
- 擅自合并其他分支
- 擅自改 M4/M7

如果工作树已有用户修改：
保留它们，并把本次改动严格控制在必要范围。

如果适合创建独立分支/工作树：
可以创建，但不要因此破坏当前工作状态。

建议本任务使用独立分支：
`ux-v3-apple-experience`

如果该分支名冲突，选择安全的新名字并记录。

---

# 20. 实施顺序

严格按以下顺序：

## Phase A：Audit
- 真实运行
- 页面/状态/路由审计
- UX问题清单
- 动效问题
- 数据安全审计

产出：
- `docs/UX_AUDIT_V3.md`
- `docs/UX_V3_IMPLEMENTATION_PLAN.md`

## Phase B：P0 体验骨架
优先：
1. 首页状态机
2. 三自然语言入口
3. 术语降维
4. 主动作层级
5. 核心页面对象连续
6. 基础 motion tokens
7. empty/loading/error

## Phase C：核心决策闭环
1. 帮我选
2. 参考图
3. 2–4 个方案比较
4. 现实可剪摘要
5. 主方案 / 备选
6. 给理发师看

## Phase D：现场与记忆
1. 十秒现场模式
2. 60 秒剪后
3. 复刻
4. 避雷
5. 下一次自动继承

## Phase E：QA
必须真实跑完整流程。

---

# 21. 测试与验收

不要用“npm test 通过”代替产品验收。

至少：

## 21.1 自动测试
- typecheck
- unit tests
- existing tests
- build
- Playwright/E2E（如果项目已有，扩充；没有则评估最小化加入）

## 21.2 Viewport
- 360
- 390
- 430
- desktop

## 21.3 Flow
完整跑：
A. 新用户 → 第一张沟通卡
B. 我有参考图 → 主方案 → 沟通卡
C. 照上次剪 → 一句话微调
D. 进入理发现场
E. 剪后 → 复刻
F. 剪后 → 避雷
G. 刷新/关闭后恢复
H. empty/error/offline
I. reduced motion

## 21.4 UX 验收
逐项确认：

- 每个屏幕有唯一主动作吗？
- 用户是否被迫理解内部术语？
- 是否有重复信息？
- 是否有装饰性无价值卡片？
- 是否可以少一步？
- 是否存在系统本可自动完成却让用户填的内容？
- 点击是否立即反馈？
- 重要对象是否保持连续？
- 返回是否自然？
- 删除/关闭规则是否可撤销？
- AI/登录占位是否诚实？
- 用户数据是否保持？

---

# 22. P80 指标

以以下目标为产品验收目标：

- 新用户 → 可用“给理发师看”：P80 <= 10 分钟
- 有满意历史 → 复刻：P80 <= 2 分钟
- 剪后最小记录：P80 <= 60 秒
- 理发师看到现场模式后：<= 10 秒能说出主方案、最在意、绝对不要
- 360 / 390 / 430 无横向溢出
- reduced-motion 有效
- 本地功能不被登录/AI/网络状态锁死

---

# 23. 强制禁止

禁止：

- 为了丰富 UI 增加无决策价值卡片。
- 首页堆功能入口。
- 长表单伪装专业。
- 各页面自创组件风格。
- 各页面自创动画风格。
- 用 Toast 代替页面真实状态变化。
- 无依据的“92%适合度”等伪精确指标。
- 固定文案冒充 AI。
- AI 覆盖用户手动事实。
- 真实 AI 未开放时创建可提交的假流程。
- 未实际打开页面就宣称 UX 完成。
- 为视觉效果破坏低端移动端性能。
- 擅自恢复 M4/M7。
- 扩展社区、支付、商家、理发师 B 端。
- reset/clean 丢失用户现有工作。

---

# 24. 完成后的交付格式

最终不要只说“已完成”。

请输出：

## 1. Product Audit 结论
- 最严重的 10 个原体验问题
- 每个问题如何修复

## 2. 本次实际改动
按用户路径列，不要按文件堆列表。

## 3. 体验前后差异
例如：
旧：点击候选 → 跳新页面
新：候选共享元素展开 → 保持上下文

## 4. 业务逻辑变化
说明：
- 状态机
- 复刻
- 避雷
- 主/备方案
- 用户手动事实 vs AI

## 5. 动效系统
说明所有 motion tokens 和适用范围。

## 6. 数据安全
说明有没有 schema 变化、migration、兼容结果。

## 7. 测试
列出真实执行命令和结果：
- build
- typecheck
- tests
- Playwright
- viewport
- reduced motion
- offline

## 8. 截图/验收证据
对核心流程保存截图或 Playwright evidence。

## 9. 未完成项
必须诚实列出。

## 10. Git
给出：
- branch/worktree
- commit SHA（如已提交）
- working tree 是否干净

---

# 25. 最后再次确认产品标准

我不要一个“看起来功能很多”的产品。

我要的是：

用户说：
> “我不知道怎么剪。”
产品帮他选。

用户说：
> “我喜欢这张图的两边，但刘海不喜欢。”
产品听得懂。

用户说：
> “上次剪得挺好，就是两边太短。”
产品下一次真的记得。

用户把手机递给理发师：
> 理发师十秒就知道怎么剪、哪里不能动。

这才是本次 V3 重构的完成标准。

现在开始：
1. 先读取仓库和文档；
2. 安全检查 Git；
3. 真实运行；
4. 完成 Audit；
5. 写 Audit/Implementation Plan；
6. 继续实施 P0；
7. 真实交互测试；
8. 最后按规定交付。

不要用“资深产品经理视角”这句话替代真实分析；用产品决策、用户路径、状态机、交互细节和可验证结果证明。
